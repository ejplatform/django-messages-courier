============
Installation
============

At the command line::

    $ easy_install django-courier

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-courier
    $ pip install django-courier
